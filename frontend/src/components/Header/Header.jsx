import React from "react";
import { StyledHeader } from "./styles";
import { BigTitle } from "../../styledComponents/styledComponents";

export default function Header() {
  return (
    <StyledHeader>
      <BigTitle>Workout Exercises</BigTitle>
    </StyledHeader>
  );
}
